/var/log/centrifuge/*log {
    create 0644 <%= node[:opsworks][:deploy_user][:user] %> <%= node[:opsworks][:deploy_user][:group] %>
    daily
    rotate 10
    missingok
    notifempty
    compress
}
